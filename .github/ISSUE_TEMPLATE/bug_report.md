---
name: Bug report
about: Something behaves incorrectly (wrong rendering, a broken page, or a boundary violation)
title: ""
labels: bug
assignees: ""
---

## What Happened

<!-- Clear description of the incorrect behavior. -->

## Expected Behavior

<!-- What should have happened instead? If this is a boundary violation — the
     console evaluating a decision, authenticating a user, calling basis-core,
     leaking a credential/token, or presenting sample data as live — say so;
     those are treated with priority. Note: suspected security issues should be
     reported privately per SECURITY.md, not as public issues. -->

## Reproduction

<!-- Steps to reproduce: which page/route, what input, what you saw. -->

1.
2.
3.

## Configuration

- Mode: <!-- sample-only / GATEWAY_BASE_URL set / GATEWAY_BEARER_TOKEN set -->
- `GATEWAY_BASE_URL` set? (do **not** paste the value):
- `GATEWAY_BEARER_TOKEN` set? (do **not** paste the token):

## Environment

- basis-console version/commit:
- Python version:
- OS / browser:

## Additional Context

<!-- Screenshots, logs (with secrets redacted), stack traces, anything else. -->
